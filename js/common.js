function over(target) {
    // 引数で受け取った要素の背景色をpinkに設定
    target.style.backgroundColor = "pink";
}

function leave(target) {
        // 引数で受け取った要素の背景色を元に戻す（透明にする）
    target.style.backgroundColor = "transparent";
}